MAILTO=""
ATST_HOME="%%ATST_HOME%%"
TARGET_MT=("land-fx")

# start MetaTrader automatically at boot
@reboot "$ATST_HOME/mtctl.sh" start "${TARGET_MT[@]}"

0 9 * * * "$ATST_HOME/check_daily.sh"
*/10 * * * * "$ATST_HOME/check_process.sh" "${TARGET_MT[@]}"
* * * * * "$ATST_HOME/check_order.sh" "${TARGET_MT[@]}"
* * * * * "$ATST_HOME/check_reboot.sh"
